[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dca-lib"
version = "0.1.0"

[tool.setuptools]
packages = ["backend", "convergence", "models", "stats", "data", "optimizers", "objectives", "tests", "fitters"]